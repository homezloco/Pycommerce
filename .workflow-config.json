{
  "name": "Start application",
  "tasks": [
    {
      "task": "shell.exec",
      "args": "python -c \"from app import app; app.run(host='0.0.0.0', port=5000, debug=True)\"",
      "waitForPort": 5000
    }
  ]
}
