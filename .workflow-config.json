{
  "name": "Start application",
  "tasks": [
    {
      "task": "shell.exec",
      "args": "cd /home/runner/workspace && python -m uvicorn web_server:app --host 0.0.0.0 --port 5000 --reload",
      "waitForPort": 5000
    }
  ]
}
